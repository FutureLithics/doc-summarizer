<!-- Navigation -->
<nav class="flex justify-between items-center mb-8">
    <div class="flex items-center space-x-6">
        <a href="/" class="text-2xl font-bold text-blue-600">📄 DocExtract</a>
        <div class="hidden md:flex space-x-4">
            <% if (locals.user) { %>
                <a href="/extractions" class="text-gray-600 hover:text-blue-600 transition-colors">Extractions</a>
                <% if (user.role === 'admin') { %>
                    <a href="/users" class="text-gray-600 hover:text-blue-600 transition-colors">Users</a>
                <% } %>
            <% } %>
        </div>
    </div>
    
    <div class="flex items-center space-x-4">
        <% if (locals.user) { %>
            <div class="flex items-center space-x-2">
                <span class="text-gray-600">Welcome, <%= user.email %></span>
                <% if (user.role === 'admin') { %>
                    <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full bg-purple-100 text-purple-800">Admin</span>
                <% } %>
            </div>
            <button id="logout-btn" class="btn-secondary">Logout</button>
        <% } else { %>
            <a href="/login" class="btn-secondary">Login</a>
            <a href="/signup" class="btn-primary">Sign Up</a>
        <% } %>
    </div>
</nav> 